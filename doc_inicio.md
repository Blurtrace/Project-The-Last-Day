 
 
 First, I used random so that I can generate a random number when calculating the population and the bad percentage.
The username is a variable that has an input so the user can enter their username. Then, I created a print that will welcome the user.
Next, I created a variable called option, where I will store a string (str) variable that is initially empty in order to use it in the while loop.
Then, I created a while loop.
This loop is used so that when the user enters a character that is different from the game level options (which are 1, 2, or 3), a message appears saying "Invalid option, please try again", and the option to select the game difficulty level is repeated.
If the user enters a valid game level number (1, 2, or 3), the condition while option not in (...) stops showing the message "Invalid option, please try again" and stops repeating the difficulty selection option.
After that, on lines 11, 22, 32, and 42, I added conditional statements such as if, elif, and else. These are used so that when the user selects an option, the resources and the population are displayed.
Finally, the print statements on lines 43, 44, 45, 46, 47, 48, 49, and 50 are used so that when the user selects the difficulty level, the program shows the resources and the population.